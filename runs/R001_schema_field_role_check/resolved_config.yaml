run:
  id: R001
  name: schema_field_role_check
  milestone: M2
  experiment_block: B1.1
  seed: 23
data:
  dataset_id: DS1_V1
  dataset_path: data/ds1_v1
  schema_version: v0
  split_id: ds1_v1_scaffold
  train_filter: train
  val_filter: validation
  test_filter: test
model:
  system_name: data_schema_sanity
  student_config: null
  baseline_config: null
training:
  optimizer: none
  learning_rate: 0.0
  batch_size: 0
  rollout_horizon: 0
  max_steps: 0
  early_stopping_metric: null
  early_stopping_patience: 0
evaluation:
  horizons: []
  metrics:
  - role_schema_checks_passed
  - leakage_checks_passed
  report_splits:
  - train
  - validation
  - test
  - held-out
  - fine-tune
  - test-window
logging:
  output_dir: runs/R001_schema_field_role_check
  save_checkpoints: false
  save_diagnostics: true
teacher_config: configs/teacher/ds1_v1.yaml
artifact_dataset_subdir: ds1
primary_metric: role_schema_checks_passed
sanity_check:
  name: schema_field_roles
