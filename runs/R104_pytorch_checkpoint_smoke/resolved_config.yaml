run:
  id: R104
  name: pytorch_checkpoint_smoke
  milestone: P3
  experiment_block: P3.5
  seed: 23
data:
  dataset_id: DS1_V1
  dataset_path: data/ds1_v1
  schema_version: v0
  split_id: ds1_v1_scaffold
  train_filter: train
  val_filter: validation
  test_filter: test
model:
  system_name: final_single_pytorch_smoke
  student_config: E2_T1_F1_S1_M0_V2_U0_pytorch_v0
  baseline_config: null
training:
  optimizer: none
  learning_rate: 0.0
  batch_size: 4
  rollout_horizon: 1
  max_steps: 0
  early_stopping_metric: torch_checkpoint_smoke_passed
  early_stopping_patience: 0
evaluation:
  horizons:
  - 1-step
  metrics:
  - torch_checkpoint_smoke_passed
  - torch_checkpoint_save_load_max_abs_diff
  report_splits:
  - train
logging:
  output_dir: runs/R104_pytorch_checkpoint_smoke
  save_checkpoints: true
  save_diagnostics: true
teacher_config: configs/teacher/ds1_v1.yaml
dataset_source: existing
artifact_dataset_subdir: ds1
primary_metric: torch_checkpoint_smoke_passed
torch_training:
  name: pytorch_checkpoint_smoke
  mode: checkpoint_smoke
  seed: 23
  split_role: train
  history_len: 8
  batch_size: 4
  max_episodes: 8
  checkpoint_name: student_model_smoke.pt
  device: cpu
  model_config:
    hidden_dim: 32
    history_len: 8
