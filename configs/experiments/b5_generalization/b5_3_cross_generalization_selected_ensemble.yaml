run: {id: R036, name: cross_generalization_selected_ensemble, milestone: M7, experiment_block: B5.3, seed: 23}
data: {dataset_id: DS1_V1, dataset_path: data/ds1_v1, schema_version: v0, split_id: ds1_v1_scaffold, train_filter: train, val_filter: validation, test_filter: held-out}
model: {system_name: cross_selected_E2_T1_F1_S1_M0_V2_U1, student_config: base_hybrid_numpy_scaffold, baseline_config: null}
training: {optimizer: ridge_regression_k3_bootstrap_ensemble, learning_rate: 0.0, batch_size: 0, rollout_horizon: 100, max_steps: 1, early_stopping_metric: cross_generalization_passed, early_stopping_patience: 0}
evaluation: {horizons: [1s, 2s, 4s], metrics: [cross_generalization_passed, rollout_rmse, uncertainty_proxy], report_splits: [seen-config, held-out-road, held-out-vehicle, test-window]}
logging: {output_dir: output/training/R036_cross_generalization_selected_ensemble, save_checkpoints: false, save_diagnostics: true}
dataset_config: /home/mi/vibe/research/simulator/configs/datasets/ds1_v1.yaml
artifact_dataset_subdir: ds1
primary_metric: cross_generalization_passed
hybrid: {success_profile: cross_generalization, seeds: [23, 29, 31], min_seed_count: 3, report_groups: [train, validation, test, seen-config, held-out-road, held-out-vehicle, test-window], horizon_steps: [25, 50, 100], history_len: 4, ridge_lambda: 10000.0, bootstrap_fraction: 0.85, variant: {encoder: E2, mu: M0-fixed, vehicle: V2-small, uncertainty: U1}}
