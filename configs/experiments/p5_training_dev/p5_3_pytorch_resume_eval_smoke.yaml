run:
  id: R109
  name: pytorch_resume_eval_smoke
  milestone: P5
  experiment_block: P5.3
  seed: 23
data:
  dataset_id: DS1_V1
  dataset_path: data/ds1_v1
  schema_version: v0
  split_id: ds1_v1_scaffold
  train_filter: train
  val_filter: validation
  test_filter: test
model:
  system_name: final_single_pytorch_resume_smoke
  student_config: E2_T1_F1_S1_M0_V2_U0_pytorch_cuda_v0
  baseline_config: null
training:
  optimizer: adamw
  learning_rate: 0.0001
  batch_size: 8
  rollout_horizon: 1
  max_steps: 10
  early_stopping_metric: torch_resume_eval_passed
  early_stopping_patience: 0
evaluation:
  horizons: [1-step]
  metrics: [torch_resume_eval_passed, torch_resume_val_mse_after]
  report_splits: [validation]
logging:
  output_dir: output/training/R109_pytorch_resume_eval_smoke
  save_checkpoints: true
  save_diagnostics: true
dataset_config: /home/mi/vibe/research/simulator/configs/datasets/ds1_v1.yaml
dataset_source: existing
artifact_dataset_subdir: ds1
primary_metric: torch_resume_eval_passed
torch_training:
  name: pytorch_resume_eval_smoke
  mode: resume_eval_smoke
  seed: 23
  split_role: train
  val_split_role: validation
  history_len: 8
  batch_size: 8
  max_episodes: 8
  max_samples: 96
  max_val_samples: 64
  sample_stride: 2
  resume_steps: 10
  eval_interval: 10
  learning_rate: 0.0001
  weight_decay: 0.0
  grad_clip: 1.0
  success_loss_ratio: 1.01
  checkpoint_name: resumed_model.pt
  checkpoint_path: output/training/R107_pytorch_one_step_training/checkpoints/model.pt
  device: cuda
  require_cuda: true
  model_config: {hidden_dim: 32, history_len: 8}
