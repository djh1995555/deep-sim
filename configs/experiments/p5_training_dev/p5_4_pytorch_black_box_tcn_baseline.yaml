run:
  id: R110
  name: pytorch_black_box_tcn_baseline
  milestone: P5
  experiment_block: P5.4
  seed: 23
data:
  dataset_id: DS1_V1
  dataset_path: data/ds1_v1
  schema_version: v0
  split_id: ds1_v1_scaffold
  train_filter: train
  val_filter: validation
  test_filter: test
model:
  system_name: black_box_tcn_pytorch_small_train
  student_config: direct_tcn_pytorch_cuda_v0
  baseline_config: direct_tcn
training:
  optimizer: adamw
  learning_rate: 0.0001
  batch_size: 8
  rollout_horizon: 1
  max_steps: 80
  early_stopping_metric: torch_black_box_training_passed
  early_stopping_patience: 0
evaluation:
  horizons: [1-step]
  metrics: [torch_black_box_training_passed, torch_one_step_training_val_mse]
  report_splits: [train, validation]
logging:
  output_dir: output/training/R110_pytorch_black_box_tcn_baseline
  save_checkpoints: true
  save_diagnostics: true
dataset_config: /home/mi/vibe/research/simulator/configs/datasets/ds1_v1.yaml
dataset_source: existing
artifact_dataset_subdir: ds1
primary_metric: torch_black_box_training_passed
torch_training:
  name: pytorch_black_box_tcn_baseline
  mode: one_step_train
  model_type: direct_tcn
  seed: 23
  split_role: train
  val_split_role: validation
  history_len: 8
  batch_size: 8
  max_episodes: 8
  max_samples: 96
  max_val_samples: 64
  sample_stride: 2
  max_steps: 80
  eval_interval: 20
  learning_rate: 0.0001
  weight_decay: 0.0
  grad_clip: 1.0
  success_loss_ratio: 0.995
  checkpoint_name: black_box_model.pt
  device: cuda
  require_cuda: true
  baseline_config:
    hidden_dim: 32
    context_dim: 17
    direct_residual_bound: 0.6
