run:
  id: R108
  name: pytorch_rollout_eval
  milestone: P5
  experiment_block: P5.2
  seed: 23
data:
  dataset_id: DS1_V1
  dataset_path: data/ds1_v1
  schema_version: v0
  split_id: ds1_v1_scaffold
  train_filter: train
  val_filter: validation
  test_filter: test
model:
  system_name: final_single_pytorch_small_train
  student_config: E2_T1_F1_S1_M0_V2_U0_pytorch_cuda_v0
  baseline_config: null
training:
  optimizer: none
  learning_rate: 0.0
  batch_size: 1
  rollout_horizon: 16
  max_steps: 0
  early_stopping_metric: torch_rollout_eval_passed
  early_stopping_patience: 0
evaluation:
  horizons: [16-step]
  metrics: [torch_rollout_eval_passed, torch_rollout_eval_rmse]
  report_splits: [validation]
logging:
  output_dir: output/training/R108_pytorch_rollout_eval
  save_checkpoints: false
  save_diagnostics: true
dataset_config: /home/mi/vibe/research/simulator/configs/datasets/ds1_v1.yaml
dataset_source: existing
artifact_dataset_subdir: ds1
primary_metric: torch_rollout_eval_passed
torch_training:
  name: pytorch_rollout_eval
  mode: rollout_eval
  seed: 23
  split_role: validation
  history_len: 8
  rollout_steps: 16
  max_episodes: 4
  max_rollout_rmse: 1000000.0
  checkpoint_path: output/training/R107_pytorch_one_step_training/checkpoints/model.pt
  device: cuda
  require_cuda: true
