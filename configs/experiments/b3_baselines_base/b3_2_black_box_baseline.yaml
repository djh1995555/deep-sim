run:
  id: R006
  name: black_box_baseline
  milestone: M4
  experiment_block: B3.2
  seed: 23
data:
  dataset_id: DS1_V1
  dataset_path: data/ds1_v1
  schema_version: v0
  split_id: ds1_v1_scaffold
  train_filter: train
  val_filter: validation
  test_filter: test
model:
  system_name: black_box_baselines
  student_config: null
  baseline_config: bb_mlp_gru_tcn_nbeatsx
training:
  optimizer: ridge_regression
  learning_rate: 0.0
  batch_size: 0
  rollout_horizon: 100
  max_steps: 1
  early_stopping_metric: validation_rollout_rmse
  early_stopping_patience: 0
evaluation:
  horizons: [1s, 2s, 4s]
  metrics: [rollout_rmse, yaw_drift, direct_multi_horizon_rmse]
  report_splits: [train, validation, test, held-out, test-window]
logging:
  output_dir: output/training/R006_black_box_baseline
  save_checkpoints: false
  save_diagnostics: true
dataset_config: configs/datasets/ds1_v1.yaml
artifact_dataset_subdir: ds1
primary_metric: black_box_baseline_passed
baseline:
  systems: [BB-MLP, BB-GRU, BB-TCN, BB-NBEATSx]
  report_splits: [train, validation, test, held-out, test-window]
  horizon_steps: [25, 50, 100]
  history_len: 4
  ridge_lambda: 0.01
