run:
  id: R005
  name: physics_only_baseline
  milestone: M4
  experiment_block: B3.1
  seed: 23
data:
  dataset_id: DS1_V1
  dataset_path: data/ds1_v1
  schema_version: v0
  split_id: ds1_v1_scaffold
  train_filter: train
  val_filter: validation
  test_filter: test
model:
  system_name: physics_only_baseline
  student_config: null
  baseline_config: physics_only
training:
  optimizer: none
  learning_rate: 0.0
  batch_size: 0
  rollout_horizon: 100
  max_steps: 0
  early_stopping_metric: null
  early_stopping_patience: 0
evaluation:
  horizons: [1s, 2s, 4s]
  metrics: [rollout_rmse, yaw_drift, constraint_violation]
  report_splits: [train, validation, test, held-out, test-window]
logging:
  output_dir: output/training/R005_physics_only_baseline
  save_checkpoints: false
  save_diagnostics: true
dataset_config: configs/datasets/ds1_v1.yaml
artifact_dataset_subdir: ds1
primary_metric: physics_only_baseline_passed
baseline:
  systems: [physics-only]
  report_splits: [train, validation, test, held-out, test-window]
  horizon_steps: [25, 50, 100]
  history_len: 4
  ridge_lambda: 0.001
