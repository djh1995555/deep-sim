run: {id: R033, name: ablation_uncertainty_U1, milestone: M6, experiment_block: B4.6, seed: 23}
data: {dataset_id: DS1_V1, dataset_path: data/ds1_v1, schema_version: v0, split_id: ds1_v1_scaffold, train_filter: train, val_filter: validation, test_filter: test}
model: {system_name: ablation_U1, student_config: base_hybrid_numpy_scaffold, baseline_config: null}
training: {optimizer: ridge_regression_k3_bootstrap_ensemble, learning_rate: 0.0, batch_size: 0, rollout_horizon: 100, max_steps: 1, early_stopping_metric: uncertainty_proxy_passed, early_stopping_patience: 0}
evaluation: {horizons: [1s, 2s, 4s], metrics: [ablation_run_passed, uncertainty_proxy], report_splits: [validation, held-out-road, held-out-vehicle]}
logging: {output_dir: output/training/R033_ablation_uncertainty_U1, save_checkpoints: false, save_diagnostics: true}
dataset_config: configs/datasets/ds1_v1.yaml
artifact_dataset_subdir: ds1
primary_metric: ablation_run_passed
hybrid: {success_profile: ablation, seeds: [23, 29, 31], min_seed_count: 3, report_groups: [train, validation, test, seen-config, held-out-road, held-out-vehicle, test-window], horizon_steps: [25, 50, 100], history_len: 4, ridge_lambda: 10000.0, bootstrap_fraction: 0.85, variant: {uncertainty: U1}}
