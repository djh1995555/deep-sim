run:
  id: R000e
  name: scenario_matrix_v1
  milestone: M1
  experiment_block: B0.6
  seed: 23
data:
  dataset_id: DS1_V1
  dataset_path: data/ds1_v1
  schema_version: v0
  split_id: ds1_v1_scaffold
  train_filter: null
  val_filter: null
  test_filter: null
model:
  system_name: ds1_scenario_matrix_generator
  student_config: null
  baseline_config: null
training:
  optimizer: none
  learning_rate: 0.0
  batch_size: 0
  rollout_horizon: 0
  max_steps: 0
  early_stopping_metric: null
  early_stopping_patience: 0
evaluation:
  horizons: []
  metrics: [full_matrix_count, scenario_groups_covered]
  report_splits: [train, validation, test, held-out, fine-tune, test-window]
logging:
  output_dir: output/training/R000e_scenario_matrix_v1
  save_checkpoints: false
  save_diagnostics: true
dataset_config: configs/datasets/ds1_v1.yaml
artifact_dataset_subdir: ds1
primary_metric: full_matrix_count
