run:
  id: R004e
  name: proxy_distribution_sanity
  milestone: M3
  experiment_block: B2.3
  seed: 31
data:
  dataset_id: DS1_PROXY_V1
  dataset_path: data/ds1_proxy_v1
  schema_version: v0
  split_id: ds1_proxy_scaffold
  train_filter: fine-tune
  val_filter: validation
  test_filter: test-window
model:
  system_name: proxy_distribution_sanity
  student_config: null
  baseline_config: null
training:
  optimizer: none
  learning_rate: 0.0
  batch_size: 0
  rollout_horizon: 0
  max_steps: 0
  early_stopping_metric: null
  early_stopping_patience: 0
evaluation:
  horizons: []
  metrics: [proxy_distribution_passed, proxy_distribution_shift_score]
  report_splits: [fine-tune, validation, test-window]
logging:
  output_dir: output/training/R004e_proxy_distribution_sanity
  save_checkpoints: false
  save_diagnostics: true
dataset_config: /home/mi/vibe/research/simulator/configs/datasets/ds1_proxy_v1.yaml
artifact_dataset_subdir: ds1_proxy
primary_metric: proxy_distribution_passed
