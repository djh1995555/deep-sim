run:
  id: R046
  name: ds2_extreme_dataset_smoke
  milestone: M10
  experiment_block: B7.1
  seed: 47
data:
  dataset_id: DS2_EXTREME_V0
  dataset_path: output/training/R046_ds2_extreme_dataset_smoke/artifacts/ds2
  schema_version: v0
  split_id: ds2_extreme_v0_scaffold
  train_filter: train
  val_filter: validation
  test_filter: test
model:
  system_name: external_simulator_ds2_extreme
  student_config: none
  baseline_config: null
training:
  optimizer: none
  learning_rate: 0.0
  batch_size: 0
  rollout_horizon: 0
  max_steps: 0
  early_stopping_metric: schema_checks_passed
  early_stopping_patience: 0
evaluation:
  horizons: [none]
  metrics: [schema_checks_passed, has_split_mu, has_transition_mu]
  report_splits: [train, validation, test, held-out]
logging:
  output_dir: output/training/R046_ds2_extreme_dataset_smoke
  save_checkpoints: false
  save_diagnostics: true
dataset_config: /home/mi/vibe/research/simulator/configs/datasets/ds2_extreme_v0.yaml
dataset_source: generate
artifact_dataset_subdir: ds2
primary_metric: schema_checks_passed
