run:
  id: R000b
  name: road_scenario_generation
  milestone: M0
  experiment_block: B0.3
  seed: 7
data:
  dataset_id: DS0_DEBUG
  dataset_path: data/ds0_debug
  schema_version: v0
  split_id: ds0_smoke
  train_filter: null
  val_filter: null
  test_filter: null
model:
  system_name: teacher_simulator_road_mu_map
  student_config: null
  baseline_config: null
training:
  optimizer: none
  learning_rate: 0.0
  batch_size: 0
  rollout_horizon: 0
  max_steps: 0
  early_stopping_metric: null
  early_stopping_patience: 0
evaluation:
  horizons: []
  metrics: [has_single_mu, has_split_mu, has_transition_mu]
  report_splits: [train]
logging:
  output_dir: runs/R000b_road_scenario_generation
  save_checkpoints: false
  save_diagnostics: true
teacher_config: configs/teacher/ds0_minimal.yaml
primary_metric: road_coverage_score
